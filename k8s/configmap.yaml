apiVersion: v1
kind: ConfigMap
metadata:
  name: shop-app-config
  namespace: shop-app
data:
  DB_HOST: postgres
  DB_PORT: "5432"
  DB_NAME: shop_db
  DB_USER: shop_user
  NODE_ENV: production
  LOG_LEVEL: info
  PORT: "3000"
